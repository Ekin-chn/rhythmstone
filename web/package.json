{
  "name": "rhythmstone-web",
  "version": "1.3.2",
  "private": true,
  "description": "Rhythm Stone web控制端，含WS联动与三卡片体验",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "npm run build && vite preview --host 0.0.0.0 --port 4173",
    "lint": "echo \"No lint step configured yet\""
  },
  "devDependencies": {
    "vite": "^5.3.3"
  }
}
